

# 📜 README - Script de Spoof para SAMP

## ❓ O que é este script?

Este é um script `.bat` que executa comandos básicos de rede no Windows com o objetivo de "limpar" ou renovar sua conexão. No contexto do **SAMP**, alguns jogadores usam isso na tentativa de **mudar seu IP dinâmico** ou **resetar a conexão** com servidores, muitas vezes tentando **burlar bans temporários por IP**, ou apenas **melhorar a conexão** em alguns casos.

⚠️ **Importante:** Esse script **não garante** que seu IP será alterado, especialmente se você usar IP fixo. Além disso, usar esse tipo de método para burlar bans pode **violar regras de servidores** e resultar em punições permanentes.

---

## 📂 Conteúdo do Script

```bat
@echo off
title rxzk
color 6
echo spoofing...
color 4
ipconfig /release
ipconfig /flushdns
ipconfig /renew
ping localhost -n 3 >nul
exit
```

### 🔍 Explicação linha por linha:

| Linha                      | O que faz                                                                 |
|---------------------------|---------------------------------------------------------------------------|
| `@echo off`               | Oculta os comandos sendo executados para manter o terminal limpo.         |
| `title rxzk`              | Muda o título da janela do terminal para "rxzk".                          |
| `color 6`                 | Altera a cor do texto do terminal (amarelo).                              |
| `echo spoofing...`        | Exibe a mensagem "spoofing..." no terminal.                               |
| `color 4`                 | Altera a cor do texto para vermelho.                                      |
| `ipconfig /release`       | Libera o endereço IP atual da máquina.                                    |
| `ipconfig /flushdns`      | Limpa o cache de DNS.                                                     |
| `ipconfig /renew`         | Solicita um novo endereço IP ao servidor DHCP.                            |
| `ping localhost -n 3 >nul`| Espera aproximadamente 3 segundos (simulando um "delay").                 |
| `exit`                    | Fecha o terminal.                                                         |

---

## 🧠 Considerações

- Esse script só é útil **em conexões com IP dinâmico**.
- Não muda o IP se você estiver usando uma **VPN, proxy ou IP fixo**.
- Pode ajudar a resolver problemas básicos de conexão ou lag momentâneo no SAMP.

---

## ⚠️ Aviso legal

Este script é apenas para **fins educacionais e técnicos**. O uso inadequado ou malicioso pode violar as regras de servidores ou termos de serviço.
